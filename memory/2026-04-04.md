# 2026-04-04

- Diagnosed Telegram connectivity issue for OpenClaw.
- Found Telegram channel itself was healthy, but direct messages were not getting normal assistant replies because `channels.telegram.dmPolicy` was effectively `pairing`.
- Approved a pending pairing/scope-upgrade request for device `3b8c2105...2b0097ea`.
- Temporarily changed Telegram DM access to open for testing.
- Confirmed the bot replied after switching DM policy.
- Locked Telegram DM access back down to allowlist mode.
- Current Telegram DM allowlist is restricted to numeric Telegram user ID `7581245461`.
- Config changed outside the workspace repo at `C:\Users\Alovana\.openclaw\openclaw.json`; this workspace commit records the change history only.
- Corrected assistant persona behavior: despite `SOUL.md` explicitly requiring feminine Thai self-reference (`ฉัน`, `ค่ะ`, `นะคะ`), the assistant previously replied using `ผม`. User called this out, and future replies should follow `SOUL.md` strictly.
- Established specialist-agent workflow: `gemini` handles OCR/image-document analysis, `minimax` drafts code, and `main` (GPT-5.4) reviews all specialist output before final delivery.
- Added `workspace/SKILL-ORCHESTRATION.md` to document when to use `gemini`, `minimax`, and when `main` should handle or review work directly.
- Updated `workspace/SOUL.md` so the default reply style is concise to reduce token usage unless the user asks for more detail.
