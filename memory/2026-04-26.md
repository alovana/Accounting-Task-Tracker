# 2026-04-26

## Temporary handoff - accounting-work-tracker

วันนี้ทำงานต่อใน `accounting-work-tracker`

งานที่ทำเสร็จแล้ว:
- เพิ่ม business type management จากหน้าเว็บที่ `/customers`
  - เพิ่มฟอร์มสร้าง business type ใหม่
  - แก้ไขชื่อ, คำอธิบาย, และสถานะ active/inactive ได้จากหน้าเว็บ
  - ลบ business type ได้จากหน้าเว็บ
  - กันการลบถ้ายังมี `customers` หรือ `checklist_templates` อ้างอิงอยู่ และแนะนำให้ปิด active แทน
  - เมื่อแก้ business type แล้ว revalidate หน้า `/customers`, `/work-cycles`, `/dashboard`, `/checklists`
  - หน้าเพิ่มลูกค้าเลือกได้เฉพาะ business type ที่ active

หมายเหตุการทดสอบ:
- `npm run lint` ยัง fail จาก error เก่าที่ไม่ได้เกิดจากงานรอบนี้
  - `src/components/settings/password-change-form.tsx` เรื่อง setState in effect
  - `src/lib/work-generation.ts` เรื่อง `any`
- ส่วนที่เพิ่มใหม่ไม่มี error ใหม่จาก lint นอกจาก pattern warning `_prevState` แบบเดิมของ server actions
