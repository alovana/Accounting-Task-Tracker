# 2026-04-27

## Temporary handoff - accounting-work-tracker

วันนี้ทำ Phase 2 ของ work cycle UX ต่อใน `accounting-work-tracker`

งานที่ทำเสร็จแล้ว:
- ปรับ `src/components/phase3/work-cycle-board.tsx` ใหม่
  - เพิ่ม view mode สลับระหว่าง `My Tasks` และ `By Customer`
  - เพิ่ม filter bar สำหรับค้นหา, ลูกค้า/รอบงาน, สถานะงาน, และกำหนดส่ง
  - เพิ่มปุ่มล้าง filters
  - เพิ่ม summary cards ระดับหน้าเพื่อสรุป visible tasks, in progress, waiting customer, blocked, overdue
  - จัดกลุ่มงานแบบพับ/ขยายตามลูกค้าเพื่อลดความยาวของหน้า
  - แสดง badges สำคัญ เช่น blocked, waiting, overdue, due soon, completed progress
  - ใน `My Tasks` จะโฟกัสเฉพาะงานของผู้ใช้ปัจจุบันสำหรับ manager/staff มากขึ้น
  - รองรับค้นหาจากชื่อลูกค้า งาน ผู้รับผิดชอบ note blocker latest update และชื่อไฟล์แนบ

ข้อสังเกต:
- เป็นการปรับ UX ที่หน้าจอเป็นหลัก ยังไม่ได้แยก route ใหม่หรือเปลี่ยน data model
- `npm run lint` ยัง fail จาก error เก่าที่ไม่ได้เกิดจากงานรอบนี้
  - `src/components/settings/password-change-form.tsx` เรื่อง setState in effect
  - `src/lib/work-generation.ts` เรื่อง `any`
